fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic>=2.12
httpx==0.27.0
python-dotenv==1.0.1
loguru==0.7.2
pydantic[email]>=2.12
email-validator==2.1.1
requests>=2.32,<3.0
sqlalchemy~=2.0.41
websockets~=15.0.1
croniter~=6.0.0
PyJWT~=2.10.1
passlib~=1.7.4
python-multipart==0.0.7
openai==1.47.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
boto3>=1.34.0
google-cloud-storage>=2.16.0
google-auth>=2.27.0
google-api-core>=2.18.0
google-resumable-media>=2.7.0
pydantic-settings>=2.0
python-socketio[asgi]==5.11.1
python-socketio[asyncio_client]==5.11.1
python-engineio==4.9.1
aiohttp>=3.9
psycopg2-binary
confluent-kafka>=2.5

# --- partner attachment pipeline ---
# Used by commands/partner/attachments/upload.py + commands/partner/_attachment_extract.py.
# All three are imported behind try/except so the upload still produces a
# metadata-only summary if the lib is missing -- but installing them is
# strongly recommended for real coverage.
pdfplumber>=0.11
python-docx>=1.1
openpyxl>=3.1

# Optional: enable LLM_PROVIDER=anthropic. Off by default so we don't pull
# the dep into images that don't need it. Uncomment to switch on.
# anthropic>=0.39

